counter=0
while read -r line;
do
	counter=$((counter+1))
	if [[ $counter -eq 2 ]]; then
		echo $line
		meanIL=$line
	elif [[ $counter -eq 6 ]]; then
		echo $line
                DISC_D=$line
	fi
done < ../results/text/bam_stats.txt

